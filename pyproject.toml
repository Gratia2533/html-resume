[project]
name = "html-resume"
version = "0.1.0"
description = "Create recruiter-friendly HTML resumes and convert them to validated A4 PDFs."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.52,<2",
]

[tool.uv]
package = false
