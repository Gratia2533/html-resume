interface:
  display_name: "HTML Resume"
  short_description: "Turn existing resumes into editable HTML and PDF"
  default_prompt: "Use $html-resume to turn my resume exported from a job platform, existing PDF, or career notes into an independently editable, recruiter-friendly HTML resume in my requested Chinese or English language without inventing facts."

policy:
  allow_implicit_invocation: true
